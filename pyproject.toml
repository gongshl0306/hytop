[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dcutop"
version = "0.7.0"
description = "nvitop-style terminal monitor for Hygon DCU (HCU) accelerators - zero-dependency, read-only, with live utilization charts and JSON output"
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
authors = [{ name = "dcutop contributors" }]
keywords = ["hygon", "dcu", "hcu", "monitor", "tui", "nvitop", "rocm-smi", "gpu"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: System :: Monitoring",
]
dependencies = []

[project.scripts]
dcutop = "dcutop.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
