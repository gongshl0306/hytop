[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hytop"
version = "0.6.0"
description = "nvitop-style terminal monitor for Hygon DCU (HCU) accelerators - zero-dependency, read-only, with live utilization charts and JSON output"
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
authors = [{ name = "hytop contributors" }]
keywords = ["hygon", "dcu", "hcu", "monitor", "tui", "nvitop", "rocm-smi", "gpu"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: System :: Monitoring",
]
dependencies = []

[project.scripts]
hytop = "hytop.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
